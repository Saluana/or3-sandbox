// Package qemu implements the VM-backed sandbox runtime using QEMU.
//
// The package supports both guest-agent and SSH-compatible control modes and is
// the primary professional isolation backend in this repository.
package qemu